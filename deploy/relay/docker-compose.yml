services:
  relay:
    build:
      context: ../..
      dockerfile: deploy/relay/Dockerfile
    restart: unless-stopped
    ports:
      - "8080:8080"
    read_only: true
    security_opt:
      - no-new-privileges:true

